# balstanok-logger

WinForms serial-port logger for capturing RX traffic from a BalStanok controller (USB-to-Serial COM port).

## Build EXE (online)
Push to `main`, then go to **Actions** and download the artifact `BalStanokLogger-win-x64`.
